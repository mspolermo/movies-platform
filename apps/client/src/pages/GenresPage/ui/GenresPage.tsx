'use client';

import { Layout } from '@/widgets/Layout';
import { AllGenres } from '@/features/getAllGenres';

export const GenresPage = () => {
  return (
    <Layout>
      <AllGenres />
    </Layout>
  );
};
