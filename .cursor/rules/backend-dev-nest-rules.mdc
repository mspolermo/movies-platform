---
description: Backend Nest — канон из .cursor/context (Sequelize, RMQ, mapper)
globs:
  - "apps/api-gateway/**"
  - "apps/kino-db/**"
  - "apps/auth-users/**"
  - "apps/common/**"
alwaysApply: false
---

# Backend (auto)

Канон проекта (при конфликте с этим файлом побеждает context/, кроме common-types):

@.cursor/context/backend.md
@.cursor/context/api.md
@.cursor/context/microservices.md
@.cursor/context/naming.md

ORM: **Sequelize** (не MikroORM). Persistence-типы — `@common/types/orm`.

## Стиль ответа

- Кратко, по-русски.
- Не ломать существующее поведение без явной задачи.
- Не удалять комментарии.
